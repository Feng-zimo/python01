# py04学习笔记

# 数据类型

| 类型    | 描述   | 示例          |
| ------- | ------ | ------------- |
| int     | 整数   | a = 1         |
| float   | 浮点数 | a = 0.1       |
| bool    | 布尔值 | a = True      |
| None    | 空值   | a = None      |
| complex | 复数   | a = 1 + 2j    |
| str     | 字符串 | a = "sixstar" |

# 字符串引号类型

| 类型     | 示例                 |
| -------- | -------------------- |
| 单引we号 | name = 'sixstar'     |
| 双引号   | name = "sixstar"     |
| 三引号   | name = """sixstar""" |

# 字符串编码

| 编码    | 描述         |
| ------- | ------------ |
| ASCII   | 英文字符     |
| GB2312  | 简体中文字符 |
| Unicode | 所有常用字符 |
| UTF-8   | 所有常用字符 |

# 字符串操作符

| 操作符 | 描述   | 示例         |
| ------ | ------ | ------------ |
| +      | 拼接   | a + b        |
| *      | 重复   | a * n        |
| [i]    | 索引   | a[i]         |
| [i:j]  | 切片   | a[i:j]       |
| in     | 包含   | 'a' in a     |
| not in | 不包含 | 'a' not in a |

# 类型检测

| 函数   | 参数 | 示例    | 输出          | 用法         |
| ------ | ---- | ------- | ------------- | ------------ |
| type() | obj  | type(a) | <class 'int'> | 检测变量类型 |

# 字符串格式化

| 方法     | 描述             | 示例                                        | 输出        | 用法         |
| -------- | ---------------- | ------------------------------------------- | ----------- | ------------ |
| %        | 百分号占位符     | "%s %s" % (name1, name2)                    | name1 name2 | 格式化字符串 |
| format() | 格式化字符串     | "{name} {age}".format(name="Alice", age=30) | Alice 30    | 格式化字符串 |
| f-string | f"{表达式}" 格式 | f"{name} {age}"                             | Alice 30    | 格式化字符串 |

# 百分号占位符

| 符号 | 描述             | 示例            | 输出   | 用法         |
| ---- | ---------------- | --------------- | ------ | ------------ |
| %s   | 字符串           | "%s" % name     | name   | 格式化字符串 |
| %d   | 整数             | "%d" % number   | number | 格式化字符串 |
| %f   | 浮点数           | "%.2f" % number | 3.14   | 格式化字符串 |
| %x   | 十六进制         | "%x" % number   | 1a     | 格式化字符串 |
| %X   | 十六进制（大写） | "%X" % number   | 1A     | 格式化字符串 |
| %o   | 八进制           | "%o" % number   | 17     | 格式化字符串 |

# 其他重要函数

| 函数         | 参数                | 示例                 | 输出  | 用法                         |
| ------------ | ------------------- | -------------------- | ----- | ---------------------------- |
| find()       | sub, __start, __end | name.find("b")       | 0     | 查找子字符串位置             |
| index()      | sub, __start, __end | name.index("b")      | 0     | 查找子字符串位置             |
| count()      | sub, __start, __end | name.count("b")      | 2     | 统计子字符串出现次数         |
| startswith() | sub, __start, __end | name.startswith("b") | True  | 检查字符串是否以子字符串开头 |
| endswith()   | sub, __start, __end | name.endswith("b")   | False | 检查字符串是否以子字符串结尾 |

# 布尔值运算

- True 相当于 1，False 相当于 0
- True 必须大写，`true` 会被当做变量名

```python
print(True + 1)   # 输出：2
print(False + 1)  # 输出：1
print(True + False) # 输出：1
```

# 变量命名规则

- 变量名由字母、数字、下划线组成
- 变量名不能以数字开头
- 变量名区分大小写
- 变量名不能是关键字

关键字列表：`and`, `as`, `assert`, `break`, `class`, `continue`, `def`, `del`, `elif`, `else`, `except`, `False`, `finally`, `for`, `from`, `global`, `if`, `import`, `in`, `is`, `lambda`, `None`, `nonlocal`, `not`, `or`, `pass`, `raise`, `return`, `True`, `try`, `while`, `with`, `yield`

# if 判断

```python
# 基本结构
if 条件:
    执行语句
elif 条件:
    执行语句
else:
    执行语句
```

注意：条件判断从上往下执行，一旦满足某个条件就不会再往下判断。精确匹配（如 `==60`）要放在范围匹配（如 `>=60`）前面。

# 运算符

| 类型       | 运算符               | 描述                     |
| ---------- | -------------------- | ------------------------ |
| 比较运算符 | `==`               | 等于                     |
|            | `!=`               | 不等于                   |
|            | `>`                | 大于                     |
|            | `<`                | 小于                     |
|            | `>=`               | 大于等于                 |
|            | `<=`               | 小于等于                 |
| 逻辑运算符 | `and`              | 两个条件都为真才返回真   |
|            | `or`               | 任一条件为真就返回真     |
|            | `not`              | 条件取反                 |
| 三元运算符 | `X if 条件 else Y` | 条件为真返回X，否则返回Y |

# while 循环

```python
# 基本结构
while 条件:
    循环体
    改变变量  # 避免死循环

# 死循环
while True:
    循环体
    if 退出条件:
        break

# while 嵌套
while 条件1:
    while 条件2:
        执行语句
```

# 字符串判断方法

| 方法      | 描述             | 示例                  | 输出 |
| --------- | ---------------- | --------------------- | ---- |
| isupper() | 字母是否全部大写 | `"HELLO".isupper()` | True |
| islower() | 字母是否全部小写 | `"hello".islower()` | True |
| isdigit() | 是否只包含数字   | `"123".isdigit()`   | True |
| isalpha() | 是否只包含字母   | `"abc".isalpha()`   | True |

# 字符串修改方法

| 方法         | 描述                                      | 示例                                | 输出                    |
| ------------ | ----------------------------------------- | ----------------------------------- | ----------------------- |
| replace()    | 替换子字符串`replace(旧, 新, 替换次数)` | `"天天向上".replace("天","时",1)` | `"时天向上"`          |
| split()      | 按指定分隔符切割字符串                    | `"Hello Python".split(" ")`       | `['Hello', 'Python']` |
| capitalize() | 首字母大写，其余小写                      | `"bingbing".capitalize()`         | `"Bingbing"`          |
| lower()      | 所有字母转小写                            | `"BINGBING".lower()`              | `"bingbing"`          |
| upper()      | 所有字母转大写                            | `"bingbing".upper()`              | `"BINGBING"`          |

> find() 找不到返回 -1，index() 找不到会报错。

# 字符串切片

语法：`[开始位置:结束位置:步长]`，包前不包后。

- 步长默认 1，绝对值决定间隔，正负号决定方向（正数从左往右，负数从右往左）

```python
st = "g79j468abc789"
print(st[0:4])   # g79j
print(st[7:])    # abc789
print(st[:7])    # g79j468
print(st[-1:])   # 9
```

# 列表

## 定义

```python
li = [1, 2, 3, 4]           # 基本定义
li = [1, 2, "hello", True]  # 可包含任意类型
```

## 常见操作

| 操作                | 描述                       | 示例                          |
| ------------------- | -------------------------- | ----------------------------- |
| `append(x)`       | 末尾添加一个元素           | `li.append('four')`         |
| `extend(list)`    | 末尾添加多个元素（传列表） | `li.extend(['five','six'])` |
| `insert(i, x)`    | 指定位置添加元素           | `li.insert(0, 'zero')`      |
| `li[i] = x`       | 通过下标修改元素           | `li[0] = 'zero'`            |
| `del li[i]`       | 删除指定下标元素           | `del li[0]`                 |
| `pop()`           | 删除末尾元素               | `li.pop()`                  |
| `pop(i)`          | 删除指定下标元素           | `li.pop(0)`                 |
| `remove(x)`       | 删除第一个匹配的元素       | `li.remove(2)`              |
| `clear()`         | 清空列表                   | `li.clear()`                |
| `sort()`          | 升序排序                   | `li.sort()`                 |
| `reverse()`       | 反转列表                   | `li.reverse()`              |
| `in` / `not in` | 判断元素是否存在           | `'one' in li`               |
| `index(x)`        | 查找元素索引               | `li.index('one')`           |
| `count(x)`        | 统计元素出现次数           | `li.count(1)`               |

## 列表推导式

```python
# 基本语法：[表达式 for 变量 in 可迭代对象]
li = [i for i in range(1, 10)]

# 带条件：[表达式 for 变量 in 可迭代对象 if 条件]
li = [i for i in range(1, 10) if i % 2 == 1]
```

## 列表嵌套

```python
li = [1, 2, [3, 4, [5, 6]]]
print(li[2][2][0])  # -> 5
```

# 元组

- 元组是不可变类型，不能修改元素
- 只有一个元素时必须加逗号：`t = (1,)`，否则 `(1)` 是整数

```python
t = (1, 2, 3, 4)
print(t[0])  # -> 1
# 支持 count(), index(), len(), in, not in
```

应用场景：函数参数和返回值、格式化输出的 `()` 本质是元组、保护数据安全。

# 字典

## 定义

```python
dic = {"name": "bingbing", "age": 18}
# 键具备唯一性（重复则后面覆盖前面），值可以重复
```

## 常见操作

| 操作                    | 描述                       | 示例                          |
| ----------------------- | -------------------------- | ----------------------------- |
| `dic[key]`            | 获取值（键不存在则报错）   | `dic['name']`               |
| `dic.get(key)`        | 获取值（键不存在返回None） | `dic.get('name')`           |
| `dic.get(key,默认值)` | 获取值（可设默认值）       | `dic.get('gender', "未知")` |
| `dic[key] = val`      | 添加/修改键值对            | `dic['name'] = "susu"`      |
| `del dic[key]`        | 删除键值对                 | `del dic['name']`           |
| `dic.pop(key)`        | 删除指定键值对             | `dic.pop('name')`           |
| `dic.popitem()`       | 删除最后一个键值对         | `dic.popitem()`             |
| `dic.clear()`         | 清空字典                   | `dic.clear()`               |
| `len(dic)`            | 键值对数量                 | `len(dic)`                  |
| `dic.keys()`          | 返回所有键                 | `dic.keys()`                |
| `dic.values()`        | 返回所有值                 | `dic.values()`              |
| `dic.items()`         | 返回所有键值对             | `dic.items()`               |

```python
for key in dic.keys():     # 遍历键
for val in dic.values():   # 遍历值
for item in dic.items():   # 遍历键值对
```

# 集合

- 元素唯一，不能重复；无序（基于哈希表实现）
- 可用于列表/元组去重

```python
set1 = {1, 2, 3, 4, 5}
set_empty = set()   # 空集合必须用 set()，{} 是空字典
```

## 常见操作

| 操作             | 描述                     |
| ---------------- | ------------------------ |
| `add(x)`       | 添加元素                 |
| `update(list)` | 添加多个元素             |
| `remove(x)`    | 删除元素（不存在报错）   |
| `discard(x)`   | 删除元素（不存在不报错） |
| `pop()`        | 随机删除元素             |
| `clear()`      | 清空集合                 |

## 集合运算

| 运算            | 描述  | 示例         |
| --------------- | ----- | ------------ |
| `set1 & set2` | 交集  | 两个集合共有 |
| `set1           | set2` | 并集         |
| `set1 - set2` | 差集  | set1 独有    |

# 类型转换

| 函数        | 描述                       | 示例                              | 注意事项                           |
| ----------- | -------------------------- | --------------------------------- | ---------------------------------- |
| `int()`   | 转整数（舍弃小数部分）     | `int(1.2)` → 1                 | 字符串必须是纯数字（允许前导+-号） |
| `float()` | 转浮点数（自动补 .0）      | `float(1)` → 1.0               | 字符串必须是纯数字                 |
| `str()`   | 转字符串（任何类型都可以） | `str(100)` → "100"             | 浮点转字符串会去掉末尾多余的0      |
| `eval()`  | 将字符串当代码执行         | `eval("10+10")` → 20           | 强大但不安全，不建议使用           |
| `list()`  | 可迭代对象转列表           | `list("123")` → ['1','2','3']  | int/float 不可转                   |
| `tuple()` | 可迭代对象转元组           | `tuple("123")` → ('1','2','3') | int/float 不可转                   |

> `eval()` 可实现 list、dict、tuple 与 str 之间的互相转换。

# 赋值与拷贝

| 方式                | 描述                       | 效果                  |
| ------------------- | -------------------------- | --------------------- |
| `li2 = li`        | 直接赋值，指向同一内存地址 | 修改 li，li2 也跟着变 |
| `li2 = li.copy()` | 浅拷贝，创建新的内存地址   | 修改 li，li2 不受影响 |

# 函数

## 函数基础

- 含义：将独立的代码组织成一个整体。使其具有特殊功能的代码块，在需要的时候再去调用
- 作用：提高代码的重用性，使整体代码看起来更加简练
- 基本格式：

```python
def 函数名():
    函数体
```

```python
def login():
    print("这是一个登录函数")
login()
# 调用几次，函数里的代码就执行几次，每次调用的时候，函数都会开始执行
```

## 编写函数示例

```python
def say_hello():
    print("Hello/你好")
    print("bingbing")
    print("age 18")
say_hello()
# 调用函数前，必须保证函数已经存在
```

## 返回值 return

- 函数执行结束后，最后给调用者的一个结果
- 作用：
  - return 会给函数的执行者提供返回值
  - 函数中遇到 return，表示此函数结束，不继续执行

```python
def buy():
    return "hamburger"
print(buy())  # -> hamburger

def buy():
    return "haburger"
    return 20  # 不会执行
# 函数中遇到return表示函数的结束，return下面的代码不会执行

def buy():
    return "haamburger", 20  # 返回一个元组
```

- 如果一个返回值都没有，返回 None
- print vs return：
  - return 表示函数结束，print 会继续执行
  - return 是返回计算值，print 是打印结果

```python
def add():
    a = 1
    b = 1
    return a + b
print(add())
```

## 参数

```python
def add(a, b):  # 形参->a,b，仅在函数内部有效
    return a * b
result = add(1, 2)  # 这时传入的参数叫做实参
# 传参 就相当于把1赋值给a,把2赋值给b
result = add(3, 4)  # -> 12
```

## 函数参数类型

### 必备参数（位置参数）

- 含义：传递和定义参数的顺序及个数必须一致

```python
def funa(name, name1, name2):
    print(name, name1, name2)
funa(1, 2, 3)
# TypeError: funa() missing 2 required positional arguments: 'name', 'name1', 'name2'
# 就是少了3个参数的意思
# 不可以多传，也不能少传，顺序要一致
# 所以叫位置参数
```

### 默认参数

- 有一个默认值
- 含义：为参数提供默认值，调用函数时可不传该默认参数的值
- 注意：所有的位置参数必须出现在默认参数前，包括函数定义和调用

```python
def func(a=8): print(a)
func()  # 没有报错
func(300)  # -> 300

def func(a=3, b=5):
    print(a, b)
func()
```

### 可变参数

- 含义：传入的值的数量是可变的，可以传入多个，也可以不传

```python
def func(*args):  # 一定要加星号*, args是约定俗成的写法
    print(args)  # 以元组形式接收
    print(type(args))
func(1, 2, 3, 4, 5, 6, 7, 8, 9)
func('asd', 'asdfdsa', 'dadsfdfsafs')
```

### 关键字参数

```python
def func(**kargs):  # kargs是约定俗成的，**代表特殊
    print(kargs)  # 以字典形式接收

func(name="bingbing", age=18)  # 传回的时候，需要采用键=值的形式
```

## 函数嵌套

### 嵌套调用

- 含义：在一个函数里调用另外一个函数

```python
def study():
    print(1)
def course():
    print(2)

def course():
    study()
    print(2)  # 这就是嵌套调用
```

### 嵌套定义

- 含义：在一个函数里定义另一个函数

```python
def study():
    print(1)
    def course():
        print(2)

def study():
    print(1)
    def course():
        print(2)
    course()
    return None
```

# 作用域

- 指变量生效的范围，分为两种，分别是全局变量和局部变量

## 全局变量

- 函数外部定义的变量，在整个文件中都是有效的

```python
a = 100
def test1():
    print("test1:", a)
def test2():
    a = 120
    print("test2:", a)

print("调用函数前a的值:", a)
test1()
test2()
print("调用函数前a的值:", a)
# a的值没有被覆盖是因为函数内部如果要用变量
# 会先在函数内部找，有的话直接使用，没有的话从外部找
```

## 局部变量

- 函数内部定义的变量，从定义位置到函数定义结束位置有效

```python
def fun():
    num123num = 10  # 局部变量num
    print(num123num)

# print(num123num)  # -> 报错  局部变量只能在被定义的函数中使用
# 作用：在函数体内部，临时保存数据，当函数调用完之后，就消除局部变量

def funa():
    num = 10
    print("funa中的num:", num)

def funb():
    num = 18
    print("funb的num:", num)

# num不冲突，因为作用域不同
funa()  # -> 10
funb()  # -> 18

# 全局变量和局部变量名称相同，全局变量不会被影响
```

## global 关键字

- 在函数内部修改全局变量
- 将变量声明为全局变量

```python
a = 10
def test():
    # global a  # 如果加上这行，则输出11
    a = 11
print(a)  # -> 10

def study():
    global name  # 将name声明为全局变量
    name = "python基础"
    print(f"我们在学习{name}。")

study()
print(name)  # 不报错

def work():
    print(name)  # 可以使用study里的变量
work()
```

## nonlocal 关键字

- 用来声明外层的局部变量，只能在嵌套函数中使用

```python
a = 10  # 全局
def a():  # 外函数
    a = 5  # 局部变量
    def b():  # 内函数
        print("b中的a值", a)
    b()
    print("a中a的值：", a)

a()  # -> 5, 5

def a():  # 外函数
    a = 5  # 局部变量
    def b():  # 内函数
        nonlocal a
        a = 20   # 对外函数进行修改
        print("b中的a值", a)
    b()
    print("a中a的值：", a)  # 输出修改过的a

a()  # -> 20, 20
```

# 匿名函数

- 函数名=lambda形参：返回值（表达式）
- 调用：结果 = 函数名（实参）

## 普通函数 vs 匿名函数

```python
# 普通函数
def add(a, b):
    return a + b
print(add(2, 3))

# 匿名函数
add = lambda a, b: a + b
print(add(13, 123))
# lambda不需要写return来返回值，表达式本身就是返回
```

## lambda 的参数形式

### 无参数

```python
funa = lambda : "123"
print(funa())  # -> 123
```

### 一个参数

```python
funa = lambda a: a
print(funa(12))  # -> 12
```

### 默认参数

```python
func = lambda a, age=18: (a, age)  # 返回值是元组形式的
print(func(12, 2314))  # -> (12, 2314)
# 默认参数必须写在非默认参数后面
```

### 关键字参数

```python
f = lambda **kwargs: kwargs
print(f(name=12, age=23, sex=452))  # 以赋值的形式来，双星号
```

### lambda 结合 if 判断

```python
a = 5
b = 8
# 三目运算
# 为真结果 if 条件 else 为假结果
print("a<b") if a < b else print("a>b")
comp = lambda a, b : "a<b" if a < b else "a>b"
# comp 比较
print(comp(5, 8))
```

# 内置函数

## 查看所有内置函数

```python
import builtins
print(dir(builtins))
# 大写字母开头一般为内置常量名，小写字母开头一般是内置函数名
```

## abs() - 返回绝对值

```python
print(abs(-10))  # -> 10   不管正负号，直接返回
print(abs(10))   # -> 10
```

## sum() - 求和

- sum里需要放可迭代对象，不能直接放数字如sum(123)，会报错

```python
print(sum([1, 2, 3]))  # -> 6
print(sum({1: 1, 2: 2, 3: 3}))  # -> 6
```

## min() / max() - 最小值/最大值

```python
print(min([1, 4, 5]))  # -> 1  最小值
print(min([-8, 5], key=abs))  # -> 5 比较两数的绝对值
# max() 最大值
```

## zip() - 打包可迭代对象

- 将可迭代对象作为参数，将对象中的元素打包成一个元组

```python
li = [1, 2, 3]
li2 = ["a", 'b', 'c']
print(zip(li, li2))
for i in zip(li, li2):
    print(i)
    # -> (1, 'a') (2, 'b') (3, 'c')
# 如果元素个数不一致，按长度最短的返回
print(list(zip(li, li2, range(5))))
```
